# 확정 설계 (Design Approved) — minicar-laptime

> 2026-08-05 · 사용자 "확정". 디자인 프리뷰 루프(4+라운드)로 확정된 **최종 모델·흐름·UI**.
> 이 문서가 Phase 3 구현의 authoritative source다 — 프리뷰 루프에서 원래 spec(project-brief
> v3·component-spec·layout-spec)과 달라진 부분을 여기서 override한다. 살아있는 참조 프로토타입:
> `_workspace/02_design/preview/index.html` (모든 동작 실측 검증됨).

## 원래 spec 대비 확정 변경 (override)

| 항목 | 원래 spec | **확정** |
|---|---|---|
| 홈 화면 | 홈(시작/지난세션) 별도 | **제거 — 랩타임(측정) 화면이 첫 화면** |
| 화면 수 | 3 (홈/측정/결과) | **2 (측정 / 기록)** |
| 측정 모델 | 연속 랩(통과마다 랩) | **단일 랩(출발→복귀 시 정지) + 누적** |
| 다중 차량 | 1-vs-rest 색상 재식별 | 유지(인식 모드) — 단, 시작이 수동/인식 **선택형** |
| 시작 | 자동/버튼 | **하단 단일 컨트롤: 탭=수동 즉시 시작 · 밀기(슬라이드)=인식 시작** |
| 정지 | — | **버튼 [정지] + 차 인식(복귀 통과) 둘 다** |
| 결과 화면 | 홈 버튼·공유·삭제 | **← 뒤로 + [초기화] 단일** (공유 제거) |
| 제목 | "미니카 랩타임" | **"랩타임"** |
| 스타일 | 테두리 있음 | **테두리 전면 제거** — 배경 명도차(app→surface→elevated)+s4 여백으로 위계 |

## 화면 1 — 랩타임 (측정, 첫 화면)

위→아래: **header(`랩타임` + 우측 `기록 N →` 흰색, 랩 있을 때만)** · 카메라 프리뷰(게이트
스트립) · 상태 배너(IDLE 준비/LEARNING/ARMED/RUNNING) · **대형 타이머**(tabular-nums, hero
스케일) · 타깃 색 칩(인식 모드 등록 후, 탭=새 타깃) · statrow(직전/Best/랩) · **하단 시작
컨트롤**.

**하단 시작 컨트롤 (단일 위젯, 두 제스처)**:
- 넓은 "시작" thumb(좌) + "밀어서 인식 시작 →" 힌트(우측 빈 공간 중앙 정렬, thumb와 비겹침).
- **탭** → 수동 시작(startMode=button): 타이머 즉시 시작.
- **슬라이드 끝까지** → 인식 시작(startMode=detect): LEARNING(~1.5s)→ARMED, 첫 통과에 출발.
- 시작 후 컨트롤 → **[정지]** 버튼(error 색). 정지 = 버튼 탭 **또는** 차 복귀 통과.
- thumb 라운드는 트랙과 동심원(트랙 r-lg 20 − 인셋 4 = 16).

**측정 상태머신**(엔진 계약과 연결): `IDLE → (탭)RUNNING(button) | (슬라이드)LEARNING→ARMED →
(첫 통과)RUNNING(detect) → (복귀 통과 또는 정지)LAP 기록 → IDLE(다음 랩 대기)`. 랩 누적.
인식 모드: 타차 통과 무시(otherPass 카운트), 경계 매칭 랩은 의심 배지. 디바운스 minGap.

## 화면 2 — 기록 (결과)

측정 화면과 **동일 디자인 언어**: header(`← 기록`(흰색) + 우측 `N랩`) · statrow 스타일 요약
(Best/평균/편차 σ) · "랩 기록" overline · **랩 리스트(좌우 s4 inset된 둥근 행, best 하이라이트,
의심 배지, tabular-nums)** · "다른 차 통과 N회 무시됨" 캡션 · 하단 **[초기화]**(error 틴트,
confirm). ← = 측정 화면 복귀. 초기화 = 세션 리셋.

## 디자인 토큰 (design-system 확정 소비)

다크 단일 팔레트(OKLCH), 배경 3단(app 0.09 / surface 0.15 / elevated 0.21), primary hue 250,
semantic success(랩 하이라이트)·warning(의심/경고)·error(정지/초기화). hero 타이머 타이포
`clamp(3.5rem,17vw,6.5rem)` tabular-nums. **테두리 미사용** — 필/배경/여백으로 구분. s4(16px)
수평 리듬. `tokens.code.css` 값 그대로 Phase 3 theme로 이관.

## Phase 3 구현 계약 (핵심)

- **엔진 계약 = "통과 이벤트 스트림"**: CV 엔진(순수 TS Worker)은 `{t(rVFC mediaTime), kind,
  signature?}` 통과 이벤트만 emit. 소비층(단일 타깃 필터/상태머신)이 UI와 분리. project-brief
  §10대로 phase2(N대) 대비 이 경계 유지.
- 실제 카메라: getUserMedia(후면) → rVFC → 전역 차분율 가림 burst 검출(임계 이원화) → 인식
  모드는 HSV 히스토그램 1-vs-rest. 타임스탬프 rVFC mediaTime.
- 상태·영속: state-contract.md의 Session/Lap 불변식(랩 연속·의심 토글·재무장 폐기·삭제 confirm)
  IndexedDB(idb). 라이브 상태(phase/타이머)는 ephemeral.
- **프로토타입의 통과 시뮬(게이트 탭·시뮬 버튼)은 구현에서 제거** — CV 엔진 자동 발화로 대체.
- 검증: 엔진 합성 fixture(feature-plan TC + brief §9) 결정론 유닛 + 상태 불변식 + browser QA.

## 승인

design-approval: 사용자 "확정"(2026-08-05). 승인 baseline = preview/index.html(프리뷰 루프
4라운드 반영본). 이후 UI 변경은 이 문서·프로토타입 갱신 후 재확인.

## R-camera-reid (2026-08-10) — 30km/h 인식 조사 반영

- **60fps**: 30km/h 통과 창(~20ms) < 30fps 간격(33ms) → 인식이 확률화. `frameRate:{ideal:60}` 요청 +
  실측 fps를 카메라 게이트 캡션에 표시(50 미만 경고색). 60fps 물리 보장 상한 ≈ 35km/h.
- **배치 계약**: 폰 높이 7cm(확정) — 레인 하나가 화면을 채워 가림율 희석 없음. 초점 불요(전역 차분).
- **무채색 시그니처**: signature = hue(24) + 무채색 밝기-대비 4 bin(Δ=luma−배경EMA, ±2T 경계).
  검정/흰/은색 차 재식별 가능. 색 칩은 무채색 peak 시 그레이스케일 스와치.
- **운영 안내**: 동일 무채색 2대는 구분 불가(원리 한계) → 유채색 스티커 권장, suspect 랩 강등 유지.
- NFC 검토 기각: 30km/h 체류(~8ms) < 트랜잭션(10~30ms), iOS Web NFC 부재, 안테나-카메라 위치 충돌.

## R2 (2026-08-10) — 실기기 1차: 감지 전멸 원인 수정

증상(실기기): 첫 인식·정지 후 이후 감지 전멸(N=0), 시작 감지도 간헐 실패.
- **원인 1 — 배경 고착**: 배경이 페이지 로드 첫 프레임에 시드된 후 재시드 없음 + 변화율이
  vibration(15%) 이상이면 배경 학습 영구 차단(주차된 차·노출 출렁임·폰 이동 시 잠금).
- **수정**: ① 카메라를 **밀어서 시작한 감지 세션 동안만** 켠다(세션마다 엔진·배경 신규 —
  구조적 해소, 사용자 확정) ② 엔진 자가복구 2종: 가림 burst > maxBurstMs(2s)면 폐기+배경
  재시드 / 중간 변화율(진동~가림)이 2s 고착돼도 재시드 ③ learning 진입 시 엔진 reset 안전망.
- **실속 완화**: 모션 블러 대비 희석 대응 — 기본 occlusion 40%→30%, pixelDelta 40→28.
- **진단 표면**: 캡션에 실측 fps + 피크 변화율/임계(도달 시 라임) 표시. URL 오버라이드
  `?occlusion=&vibration=&delta=&minGap=&maxBurst=`로 배포 없이 현장 재조정.
- 수동(탭) 모드는 카메라 미사용 — 버튼 정지 전용(사용자 확정으로 기존 "수동+차 인식 정지" 대체).

## R3 (2026-08-10) — 실기기 2차: 매칭 계층이 정지·재시작을 막던 문제

증상: R2 후에도 복귀 정지 안 됨 + 정지 후 재시작 안 됨.
- **원인 — 매칭 과엄격**: ① 타깃 시그니처가 세션을 넘어 영구 보존 → 새 세션(새 카메라·노출)의
  첫 통과가 낡은 타깃과 매칭 실패 → 시작 거부 ② 실속 블러·노출 변화로 같은 차의 통과 간
  L1 거리가 BORDER(1.0) 초과 → 복귀 통과가 "타차"로 무시 → 정지 안 됨.
- **수정**: ① 감지 세션마다 타깃 재등록(startDetect에서 targetSig 초기화) — 세션 간 정체성은
  "내 차를 먼저 출발"이라는 운영 규칙이 담당, 시그니처는 주행 중 타차 무시(1-vs-rest 본연)
  전용 ② SIG_BORDER 1.0→1.5 (동색 블러 변형 흡수, 이색 ≈1.8~2.0은 계속 구분; 경계는 의심 랩)
  ③ ?match=&border= URL 오버라이드로 현장 조정.

## R4 (2026-08-10) — 실기기 3차: 자가 진단 표면 + 카메라 준비 게이트

- **카메라 준비 게이트**: LEARN 타이머를 슬라이드 즉시가 아니라 **스트림 확립(cameraReady) 후**
  시작 — 카메라가 늦게 켜지는 기기에서 스트림 없이 armed가 되고 첫 프레임(배경 시드)이 주행
  중인 차 위에서 찍히던 레이스 제거. learning 중 카메라 미확립이면 배너 "카메라 켜는 중…".
- **진단 표면 3종**: ① 카메라 실패를 침묵하지 않고 캡션에 사유 노출(권한/미지원 등)
  ② 캡션 미터에 누적 프레임 수(f N — 0 고착이면 파이프 사망) ③ 배너에 판정 로그 1줄
  (출발/정지/타차 d=…/디바운스) — 상태머신이 이벤트를 버릴 때 그 사유가 화면에 보인다.

## R5 (2026-08-10) — 실기기 근본 원인 확정: iOS mediaTime 동결

스크린샷 증거: 60fps 표시·프리뷰 정상인데 `f1`·피크 0% 고정 + "출발 — 타깃 등록"은 발생.
→ 프레임은 흐르지만 **rVFC meta.mediaTime이 getUserMedia 스트림에서 전진하지 않음**(iOS).
엔진의 모든 시간 게이트(minGap 300ms·maxBurst 2s·스탯 250ms)가 동결돼 첫 통과만 성공
(-∞ 기준)하고 이후 통과는 영구 디바운스 — "시작은 되는데 정지 안 됨 + N=0"의 근본 원인.
수정: 프레임 타임스탬프 정본을 rVFC/rAF 콜백 시각(now)으로 교체(60fps에서 ±8ms, 오차 예산 내).

## R6 (2026-08-10) — 인식률 향상: 120fps + soft 과도 감지 (+조명 가이드)

- **조명(운영)**: 레인 측면광으로 노출 단축 → 블러·시그니처 열화 감소. 설치 후 손 흔들어
  피크 미터(라임 도달)로 즉석 캘리브레이션.
- **120fps ideal 요청**: 미지원 기기는 자동 하향(무해). 통과 창에 2~4프레임, 보장 상한 70km/h.
  실효 콜백은 디스플레이 주사율에 묶임(ProMotion에서 최대 효과) — 캡션 실측 fps로 확인.
- **soft 과도 감지**: 단일 프레임 30% 외에 "연속 2~5프레임 ≥18% 후 하강"의 짧은 과도도 통과로
  인정 — 실속에서 경계 프레임 2장이 20%대에 걸리던 놓침 회수. 지속 교란(노출 출렁임)은
  기각+억제(조용해져야 재개), soft 선행 에지는 hard burst에 합류해 타이밍 정밀도도 개선.
  오버라이드: ?soft=0.18.

## R7 (2026-08-10) — 카메라 전체 배경 (사용자 제안)

상단 프리뷰 스트립 제거 — 카메라가 켜지면(감지 세션) 화면 전체 배경으로 표시(object-fit
cover + 상하 진한 그라데이션 스크림으로 타이머 가독성 확보, 0.3s 페이드인). 꺼지면 기존
다크 배경. 진단 캡션(fps·피크·f)은 헤더 아래 lens-line으로 이동. 배치 시 레인 정렬을
크게 보며 맞출 수 있는 실용 이점. 캡처 ROI는 비디오 원본 기준이라 엔진 영향 없음.

## R8 (2026-08-11) — 시그니처 스펙트럼 확장 (사용자 제안: 다색 판단력)

- **hue×밝기 2평면(52bin)**: [어두운 톤 24][밝은 톤 24][무채색 4]. 밝기 분류는 luma가 아닌
  **value(max RGB)** — 파랑 계열은 luma가 태생적으로 낮아 luma로는 남색/하늘색이 안 갈라짐.
  경계 픽셀은 value ramp(0.38~0.63)로 두 평면에 소프트 배분(노출 변화 널뛰기 방지).
- **원형 hue 스무딩**: finalize 시 평면별 3-탭 [0.25,0.5,0.25] — AWB 드리프트로 인접 bin에
  밀려도 같은 차 거리 완화(인접 bin L1 2.0→1.0). 무채색 bin은 순서형이라 미적용.
- 칩 색: 어두운 평면 peak면 낮은 명도로 표시(남색/하늘색 칩 구분).
- 매칭 임계(0.7/1.5) 유지 — 합성 검증: 남색↔남색 <0.3, 남색↔하늘색 >1.5, 인접 hue 드리프트 <1.2.
- 알려진 한계(테스트가 재현): 배경과 luma가 비슷한 색은 전경 차분에서 빠짐 — 색 차분 감지(§5)는
  실사용 증거 발생 시 별도 라운드.

## R9 (2026-08-11) — 색 차분(대립채널) 감지

전경 판정을 luma 단독 → **luma OR 대립채널 색 차분**으로 확장. 대립채널(r−g, g−b)은 전 채널
동일 이동(순수 노출 변화)에 불변이라 AE 출렁임에는 반응하지 않고, 배경과 밝기가 같은 색 차
(하늘색 차 + 동명도 회색 트랙 — R8 테스트가 재현한 사각지대)만 회수한다. 배경 모델에 대립채널
2평면 EMA 추가, 시드/자가복구/학습이 luma와 동행. 시그니처 전경 필터도 동일 판정 공유.
chromaDeltaThreshold 48 (두 항 합산 — 센서 노이즈·경미한 AWB 이동은 미달), ?chromaDelta= 오버라이드.
rgb 미제공 프레임은 luma 전용(하위 호환).

## R10 (2026-08-11) — 의심 정지 유예 (실기기: 타차 의심 통과가 타이머를 끊음)

의심 구간(0.7<d≤1.5) 통과는 즉시 정지하지 않고 2s 유예 동안 확정 매치(d≤0.7)를 기다린다.
확정이 오면 그것으로 정지(의심=타차였음), 안 오면 의심 통과의 **원래 tMs**로 의심 랩 정지 —
랩타임은 통과 시각 기준이라 유예가 기록을 왜곡하지 않음(표시만 최대 2s 늦게 확정). 유예 중
더 작은 d의 의심이 오면 교체. 해제 경로: 확정 정지·버튼 정지·cancel·rearm·reset.
?grace=0이면 종전 즉시 의심 정지. 판정 로그에 "의심 통과 d=… — 2.0s 확정 대기" 노출.

### R10-b (2026-08-11) — 의심 처리 사용자 확정 흐름 (유예 창 폐기)

"확정 매치가 2초 안에 올 일은 없다"(사용자) → 유예 창 제거. 확정 흐름:
① 의심 통과 = **시각만 보류, 타이머 계속** ② 이후 확정 매치 오면 확정으로 정지 + 의심 삭제
③ 확정 없이 버튼 정지하면 보류된 의심 시각으로 랩 기재 + 의심 표시. 후속 의심은 최초 의심을
대체하지 않음(먼저 통과한 쪽이 결승 후보). ?grace= 오버라이드 폐기.

### R10-c (2026-08-11) — 다중 의심 전부 시간순 표시 (사용자 확정)

의심이 여러 건이면 전부 보류 목록에 시간순으로 쌓는다. 확정 없이 버튼 정지 시: 채택값 =
최초 유효 의심(랩 하한 이상), 랩에 `candidatesMs`(시간순 전부)를 실어 기록 화면에
"의심 후보(시간순): 2.00 · 3.50 · 5.20" 형태로 표기 — 어느 통과가 내 차였는지 사용자가 판단.
Lap.candidatesMs는 additive optional(구 레코드 안전). 후보 1건이면 목록 생략(durationMs가 곧 그 값).

### R10-d (2026-08-11) — 의심 결산은 detect 모드 전용 (사용자 확정)

버튼 정지의 의심 결산(최초 의심 시각 채택 + 후보 표기)은 **밀어서 인식 시작(detect)일 때만**.
수동(탭) 시작의 버튼 정지는 항상 클릭 시점 기록 — 수동은 카메라가 꺼져 있어 의심이 생길 수
없지만 startMode 가드로 계약을 명시 고정.

## R-hybrid-1 (2026-08-11) — Capacitor iOS 셸

네이티브 하이브리드 1단계(사용자 확정 로드맵). Capacitor 8.5(SPM — CocoaPods 불요),
appId dev.taese83.laptime, webDir dist. ios/App = Xcode 프로젝트(커밋), dist 복사본·빌드
산출물은 Capacitor 기본 .gitignore가 제외. NSCameraUsageDescription 추가. safe-area
패딩(.bar/.bottom-cta — 웹은 env=0이라 무변화, 실측 12/16px 확인). 웹 배포(Vercel) 공존.
다음: ② HighFpsCamera 플러그인(240fps+노출/AWB 잠금+네이티브 다운스케일).
빌드·서명은 Xcode.app 필요(이 맥 미설치 — 사용자 설치 후 진행).

## R-hybrid-2 (2026-08-11) — HighFpsCamera 네이티브 플러그인

- **Swift 플러그인**(ios/App/App/HighFpsCameraPlugin.swift): 후면 카메라 최고 fps 포맷(목표 240,
  미지원 시 자동 하향) + 시작 1.4s 후 노출/화이트밸런스 잠금(웹 R2·R8 보정 대상의 원천 제거)
  + 네이티브 64×48 다운스케일(luma 공식 웹과 동일) + 하드웨어 타임스탬프 + 초당 ~60 메시지
  배치 전송 + WebView 투명화·네이티브 프리뷰 레이어(R7 전체 배경 UX 유지).
- **JS 어댑터**(camera-native.ts): CameraHandle 계약 동일 — Worker 엔진·상태머신·임계 무수정.
  MeasureScreen이 Capacitor.isNativePlatform()으로 분기(웹 경로 불변). 등록은
  AppViewController(capacitorDidLoad → registerPluginInstance).
- 검증: 웹 게이트(typecheck·27/27·build) + xcodebuild 시뮬레이터 BUILD SUCCEEDED.
  실기기 확인 항목: 캡션 fps=240/120 표시, 잠금 후 화면 밝기 고정, 통과 인식·타이밍.

### R-hybrid-2 fix (2026-08-11) — "plugin is not implemented" 원인과 해결

시뮬레이터 재현·계층 추적으로 확정: Capacitor 8 스캐폴드는 **SceneDelegate가 코드로
CAPBridgeViewController()를 생성** — 스토리보드의 커스텀 클래스 지정은 무효 경로였고
capacitorDidLoad(플러그인 등록)가 불리지 않았다. SceneDelegate에서 AppViewController()를
직접 생성하도록 수정. 시뮬레이터 실증: 등록 로그 → 권한 팝업(우리 문구) → CamError.noDevice
(시뮬레이터 무카메라 정상 실패) — JS→네이티브 왕복 완성. 참고: Xcode 증분 빌드가 새 파일을
빼먹는 사례 확인 — 네이티브 파일 추가 후엔 Clean Build Folder 권장. packageClassList 주입은
cap sync가 덮어써 불가(기록).

### R11 (2026-08-11) — 시작 직후 정지 버튼 씹힘 수정

원인: recordLap의 랩 하한(800ms) 디바운스가 **버튼 정지까지** 거부하며 running을 유지 —
디바운스는 감지 이중 트리거 방어이지 사용자 버튼용이 아니다. 수정: 버튼 정지는 항상 반응 —
하한 미만이면 **기록 없이 종료**(시작 직후 오조작 = 취소 시맨틱, 판정 로그 "랩 하한 미만,
기록 없음"). 수동/감지 공통.
